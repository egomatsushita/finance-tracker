services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file: .env
    environment:
      UVICORN_HOST: 0.0.0.0
      UV_PROJECT_ENVIRONMENT: /opt/venv
      UV_LINK_MODE: copy
    command: sh -c "uv run alembic upgrade head && uv run app/main.py"
